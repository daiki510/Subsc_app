<%= @user.name %>さん

合計利用料金：<%= @user.subscriptions.map { |subscription| subscription.charge}.sum %>円

利用サブスクリプション一覧
<% @user.subscriptions.each do |subscription| %>
  <%# どこかで変数定義するかメソッド化する %>
  <% service = Service.find_by(id: subscription.service_id) %>
    サブスクリプション：<%= service.name %>
    利用料金：<%= subscription.charge %>円
    支払日：毎月<%= subscription.due_date %>
<% end %>